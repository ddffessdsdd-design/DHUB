--// HIDDEN STYLE ADMIN PANEL
--// Put this inside StarterPlayerScripts as a LocalScript

local Players = game:GetService("Players")
local Lighting = game:GetService("Lighting")
local UIS = game:GetService("UserInputService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

---------------------------------------------------
-- SETTINGS
---------------------------------------------------

local Settings = {
    SurvivorESP = false,
    KillerESP = false,
    GeneratorESP = false,
    EscapeESP = false,
    TrapESP = false,
    Fullbright = false,
    InstantPrompts = false,
    WalkSpeed = 16,
}

---------------------------------------------------
-- GUI
---------------------------------------------------

local gui = Instance.new("ScreenGui")
gui.Name = "HiddenUI"
gui.ResetOnSpawn = false
gui.Parent = playerGui

---------------------------------------------------
-- OPEN BUTTON
---------------------------------------------------

local openButton = Instance.new("TextButton")
openButton.Size = UDim2.new(0,140,0,45)
openButton.Position = UDim2.new(0,20,1,-65)
openButton.BackgroundColor3 = Color3.fromRGB(20,20,28)
openButton.Text = "Open Panel"
openButton.Font = Enum.Font.GothamBold
openButton.TextSize = 18
openButton.TextColor3 = Color3.new(1,1,1)
openButton.Parent = gui

Instance.new("UICorner", openButton).CornerRadius = UDim.new(0,12)

local openStroke = Instance.new("UIStroke")
openStroke.Color = Color3.fromRGB(70,70,90)
openStroke.Parent = openButton

local openGradient = Instance.new("UIGradient")
openGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(80,60,255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(120,90,255))
}
openGradient.Rotation = 45
openGradient.Parent = openButton

---------------------------------------------------
-- MAIN
---------------------------------------------------

local main = Instance.new("Frame")
main.Size = UDim2.new(0, 1000, 0, 580)
main.Position = UDim2.new(0.5, -500, 0.5, -290)
main.BackgroundColor3 = Color3.fromRGB(10,10,15)
main.BorderSizePixel = 0
main.Visible = false
main.Parent = gui

Instance.new("UICorner", main).CornerRadius = UDim.new(0,18)

local mainStroke = Instance.new("UIStroke")
mainStroke.Color = Color3.fromRGB(40,40,50)
mainStroke.Parent = main

local mainGradient = Instance.new("UIGradient")
mainGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(15,15,22)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(8,8,12))
}
mainGradient.Rotation = 90
mainGradient.Parent = main

---------------------------------------------------
-- OPEN/CLOSE
---------------------------------------------------

openButton.MouseButton1Click:Connect(function()

    main.Visible = not main.Visible

    if main.Visible then
        openButton.Text = "Close Panel"
    else
        openButton.Text = "Open Panel"
    end
end)

---------------------------------------------------
-- DRAGGING
---------------------------------------------------

local dragging
local dragInput
local dragStart
local startPos

local function update(input)

    local delta = input.Position - dragStart

    main.Position = UDim2.new(
        startPos.X.Scale,
        startPos.X.Offset + delta.X,
        startPos.Y.Scale,
        startPos.Y.Offset + delta.Y
    )
end

main.InputBegan:Connect(function(input)

    if input.UserInputType == Enum.UserInputType.MouseButton1 then

        dragging = true
        dragStart = input.Position
        startPos = main.Position

        input.Changed:Connect(function()

            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

main.InputChanged:Connect(function(input)

    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

UIS.InputChanged:Connect(function(input)

    if input == dragInput and dragging then
        update(input)
    end
end)

---------------------------------------------------
-- TITLE
---------------------------------------------------

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1,0,0,60)
title.BackgroundTransparency = 1
title.Text = "Hidden - Your Game"
title.Font = Enum.Font.GothamBold
title.TextSize = 30
title.TextColor3 = Color3.new(1,1,1)
title.Parent = main

---------------------------------------------------
-- SIDEBAR
---------------------------------------------------

local sidebar = Instance.new("Frame")
sidebar.Size = UDim2.new(0,90,1,-80)
sidebar.Position = UDim2.new(0,15,0,65)
sidebar.BackgroundColor3 = Color3.fromRGB(15,15,20)
sidebar.Parent = main

Instance.new("UICorner", sidebar).CornerRadius = UDim.new(0,18)

local sidebarGradient = Instance.new("UIGradient")
sidebarGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(22,22,30)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(12,12,18))
}
sidebarGradient.Rotation = 90
sidebarGradient.Parent = sidebar

local sideLayout = Instance.new("UIListLayout")
sideLayout.Padding = UDim.new(0,12)
sideLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
sideLayout.Parent = sidebar

local sidePad = Instance.new("UIPadding")
sidePad.PaddingTop = UDim.new(0,20)
sidePad.Parent = sidebar

local function makeTab(name)

    local button = Instance.new("TextButton")
    button.Size = UDim2.new(0,58,0,58)
    button.BackgroundColor3 = Color3.fromRGB(20,20,28)
    button.Text = name
    button.Font = Enum.Font.GothamBold
    button.TextSize = 14
    button.TextColor3 = Color3.new(1,1,1)
    button.Parent = sidebar

    Instance.new("UICorner", button).CornerRadius = UDim.new(1,0)

    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(85,65,255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(50,40,140))
    }
    gradient.Rotation = 45
    gradient.Parent = button

    return button
end

makeTab("ESP")
makeTab("PLY")
makeTab("WRLD")
makeTab("UTIL")

---------------------------------------------------
-- CONTENT
---------------------------------------------------

local content = Instance.new("Frame")
content.Size = UDim2.new(1,-130,1,-90)
content.Position = UDim2.new(0,120,0,70)
content.BackgroundTransparency = 1
content.Parent = main

---------------------------------------------------
-- CARD FUNCTION
---------------------------------------------------

local function createCard(titleText, x, y, w, h)

    local card = Instance.new("Frame")
    card.Size = UDim2.new(0,w,0,h)
    card.Position = UDim2.new(0,x,0,y)
    card.BackgroundColor3 = Color3.fromRGB(16,16,22)
    card.BorderSizePixel = 0
    card.Parent = content

    Instance.new("UICorner", card).CornerRadius = UDim.new(0,16)

    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(22,22,30)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(14,14,18))
    }
    gradient.Rotation = 90
    gradient.Parent = card

    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(35,35,45)
    stroke.Parent = card

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1,-20,0,30)
    title.Position = UDim2.new(0,15,0,10)
    title.BackgroundTransparency = 1
    title.Text = titleText
    title.Font = Enum.Font.GothamBold
    title.TextSize = 24
    title.TextColor3 = Color3.new(1,1,1)
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Parent = card

    return card
end

---------------------------------------------------
-- TOGGLE FUNCTION
---------------------------------------------------

local function createToggle(parent, text, yPos, callback)

    local holder = Instance.new("Frame")
    holder.Size = UDim2.new(1,-30,0,40)
    holder.Position = UDim2.new(0,15,0,yPos)
    holder.BackgroundTransparency = 1
    holder.Parent = parent

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7,0,1,0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.Font = Enum.Font.Gotham
    label.TextSize = 17
    label.TextColor3 = Color3.new(1,1,1)
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = holder

    local toggle = Instance.new("TextButton")
    toggle.Size = UDim2.new(0,50,0,26)
    toggle.Position = UDim2.new(1,-55,0.5,-13)
    toggle.BackgroundColor3 = Color3.fromRGB(40,40,50)
    toggle.Text = ""
    toggle.Parent = holder

    Instance.new("UICorner", toggle).CornerRadius = UDim.new(1,0)

    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0,20,0,20)
    knob.Position = UDim2.new(0,3,0.5,-10)
    knob.BackgroundColor3 = Color3.new(1,1,1)
    knob.Parent = toggle

    Instance.new("UICorner", knob).CornerRadius = UDim.new(1,0)

    local enabled = false

    toggle.MouseButton1Click:Connect(function()

        enabled = not enabled

        if enabled then

            toggle.BackgroundColor3 = Color3.fromRGB(80,60,255)
            knob.Position = UDim2.new(1,-23,0.5,-10)

        else

            toggle.BackgroundColor3 = Color3.fromRGB(40,40,50)
            knob.Position = UDim2.new(0,3,0.5,-10)
        end

        callback(enabled)
    end)
end

---------------------------------------------------
-- ESP FUNCTION
---------------------------------------------------

local function createESP(target, color, text)

    if target:FindFirstChild("ADMIN_HIGHLIGHT") then
        return
    end

    local highlight = Instance.new("Highlight")
    highlight.Name = "ADMIN_HIGHLIGHT"
    highlight.FillColor = color
    highlight.FillTransparency = 0.7
    highlight.OutlineTransparency = 1
    highlight.Parent = target

    local adornee =
        target:FindFirstChild("HumanoidRootPart")
        or target.PrimaryPart
        or target:FindFirstChildWhichIsA("BasePart")

    if adornee then

        local billboard = Instance.new("BillboardGui")
        billboard.Size = UDim2.new(0,100,0,40)
        billboard.StudsOffset = Vector3.new(0,4,0)
        billboard.AlwaysOnTop = true
        billboard.Parent = adornee

        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1,0,1,0)
        label.BackgroundTransparency = 1
        label.TextScaled = true
        label.Font = Enum.Font.GothamBold
        label.TextColor3 = color
        label.Text = text
        label.Parent = billboard
    end
end

---------------------------------------------------
-- ESP SCAN
---------------------------------------------------

local function scanESP()

    if Settings.SurvivorESP then

        local aliveFolder = workspace:FindFirstChild("PLAYERS")
            and workspace.PLAYERS:FindFirstChild("ALIVE")

        if aliveFolder then

            for _, char in pairs(aliveFolder:GetChildren()) do
                createESP(char, Color3.fromRGB(0,255,0), "SURVIVOR")
            end
        end
    end

    if Settings.KillerESP then

        local killerFolder = workspace:FindFirstChild("PLAYERS")
            and workspace.PLAYERS:FindFirstChild("KILLER")

        if killerFolder then

            for _, char in pairs(killerFolder:GetChildren()) do
                createESP(char, Color3.fromRGB(255,0,0), "KILLER")
            end
        end
    end

    if Settings.GeneratorESP then

        local gens = workspace.MAPS["GAME MAP"].Tasks.Generators

        for _, gen in pairs(gens:GetChildren()) do

            if gen.Name == "Generator" then
                createESP(gen, Color3.fromRGB(150,150,150), "GENERATOR")
            end
        end
    end

    if Settings.EscapeESP then

        local escapes = workspace.MAPS["GAME MAP"].Escapes

        for _, obj in pairs(escapes:GetChildren()) do

            if obj.Name == "EscapePoint" then
                createESP(obj, Color3.fromRGB(255,255,0), "ESCAPE")
            end
        end
    end

    if Settings.TrapESP then

        for _, obj in pairs(workspace:GetDescendants()) do

            if obj.Name == "BearTrap" then
                createESP(obj, Color3.fromRGB(255,120,120), "TRAP")
            end
        end
    end
end

---------------------------------------------------
-- FULLBRIGHT
---------------------------------------------------

local function updateFullbright()

    if Settings.Fullbright then

        Lighting.Brightness = 5
        Lighting.ClockTime = 14
        Lighting.FogEnd = 100000

    else

        Lighting.Brightness = 1
    end
end

---------------------------------------------------
-- PROMPTS
---------------------------------------------------

local function updatePrompts()

    for _, obj in pairs(workspace:GetDescendants()) do

        if obj:IsA("ProximityPrompt") then

            if Settings.InstantPrompts then
                obj.HoldDuration = 0
            end
        end
    end
end

---------------------------------------------------
-- SPEED
---------------------------------------------------

local function updateSpeed()

    local char = player.Character
    if not char then return end

    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum then return end

    hum.WalkSpeed = Settings.WalkSpeed
end

---------------------------------------------------
-- ESP CARD
---------------------------------------------------

local espCard = createCard("ESP Features", 0, 0, 420, 330)

createToggle(espCard, "Survivor ESP", 60, function(v)
    Settings.SurvivorESP = v
end)

createToggle(espCard, "Killer ESP", 105, function(v)
    Settings.KillerESP = v
end)

createToggle(espCard, "Generator ESP", 150, function(v)
    Settings.GeneratorESP = v
end)

createToggle(espCard, "Escape ESP", 195, function(v)
    Settings.EscapeESP = v
end)

createToggle(espCard, "Trap ESP", 240, function(v)
    Settings.TrapESP = v
end)

---------------------------------------------------
-- WORLD CARD
---------------------------------------------------

local worldCard = createCard("World", 440, 0, 420, 170)

createToggle(worldCard, "Fullbright", 60, function(v)
    Settings.Fullbright = v
    updateFullbright()
end)

createToggle(worldCard, "Instant Prompts", 110, function(v)
    Settings.InstantPrompts = v
    updatePrompts()
end)

---------------------------------------------------
-- PLAYER CARD
---------------------------------------------------

local playerCard = createCard("Player", 440, 190, 420, 150)

local speedButton = Instance.new("TextButton")
speedButton.Size = UDim2.new(0,180,0,45)
speedButton.Position = UDim2.new(0,20,0,70)
speedButton.BackgroundColor3 = Color3.fromRGB(80,60,255)
speedButton.Text = "Toggle Speed"
speedButton.Font = Enum.Font.GothamBold
speedButton.TextSize = 18
speedButton.TextColor3 = Color3.new(1,1,1)
speedButton.Parent = playerCard

Instance.new("UICorner", speedButton).CornerRadius = UDim.new(0,12)

local speedGradient = Instance.new("UIGradient")
speedGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(85,65,255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(55,40,160))
}
speedGradient.Rotation = 45
speedGradient.Parent = speedButton

speedButton.MouseButton1Click:Connect(function()

    if Settings.WalkSpeed == 16 then
        Settings.WalkSpeed = 32
    else
        Settings.WalkSpeed = 16
    end

    updateSpeed()
end)

---------------------------------------------------
-- LOOP
---------------------------------------------------

while task.wait(2) do

    pcall(function()

        scanESP()
        updateSpeed()

    end)
end
